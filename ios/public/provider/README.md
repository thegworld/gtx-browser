This directory exists to allow GTx Browser code to call iOS code that is not yet
upstreamed. The main entry point from GTx Browser into this directory is via
global Provider objects. These objects play a role analogous to that of the
Client objects in the content layer.
